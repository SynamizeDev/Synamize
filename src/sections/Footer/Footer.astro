---
const year = new Date().getFullYear();

const quickLinks = [
  { label: 'Home', href: '#' },
  { label: 'Services', href: '#services' },
  { label: 'Results', href: '#testimonials' },
  { label: 'About', href: '#team' },
  { label: 'Contact', href: '#contact' },
];

const serviceLinks = [
  { label: 'Social Media', href: '#services' },
  { label: 'Paid Ads', href: '#services' },
  { label: 'Content Creation', href: '#services' },
  { label: 'Websites', href: '#services' },
  { label: 'Growth Systems', href: '#services' },
];

const connectLinks = [
  { label: 'Email', href: 'mailto:hello@synamize.agency' },
  { label: 'Instagram', href: 'https://instagram.com/' },
  { label: 'LinkedIn', href: 'https://linkedin.com/' },
];
---

<footer id="contact" class="footer" aria-labelledby="footer-cta-title">
  <div class="container footer__wrap">
    <div class="footer__panel">
      <div class="footer__cta">
        <div class="footer__cta-copy">
          <h2 id="footer-cta-title" class="footer__cta-title">
            Your Next Customer Is Already Online
          </h2>
          <p class="footer__cta-text">
            Discover how content, advertising, and digital systems can help your business attract more customers and grow consistently.
          </p>
        </div>
        <a href="mailto:hello@synamize.agency" class="btn btn--gradient footer__cta-btn">
          Let's Talk Growth
        </a>
      </div>

      <div class="footer__rule" aria-hidden="true"></div>

      <div class="footer__body">
        <div class="footer__columns">
          <nav class="footer__col" aria-label="Quick links">
            <p class="footer__col-title">Quick Links</p>
            <ul class="footer__links">
              {quickLinks.map((link) => (
                <li>
                  <a href={link.href} class="footer__link">{link.label}</a>
                </li>
              ))}
            </ul>
          </nav>

          <nav class="footer__col" aria-label="Services">
            <p class="footer__col-title">Services</p>
            <ul class="footer__links">
              {serviceLinks.map((link) => (
                <li>
                  <a href={link.href} class="footer__link">{link.label}</a>
                </li>
              ))}
            </ul>
          </nav>

          <nav class="footer__col" aria-label="Connect">
            <p class="footer__col-title">Connect</p>
            <ul class="footer__links">
              {connectLinks.map((link) => (
                <li>
                  <a
                    href={link.href}
                    class="footer__link"
                    {...(link.href.startsWith('http') ? { target: '_blank', rel: 'noopener noreferrer' } : {})}
                  >
                    {link.label}
                  </a>
                </li>
              ))}
            </ul>
          </nav>
        </div>
      </div>

      <div class="footer__rule" aria-hidden="true"></div>

      <p class="footer__copy">&copy; Synamize&nbsp;&nbsp;|&nbsp;&nbsp;{year} All Rights Reserved</p>
    </div>
  </div>
</footer>

<style>
  .footer {
    padding-block: calc(var(--space-section) / 2) clamp(2.5rem, 6vw, 4rem);
    background: transparent;
  }

  .footer__wrap {
    width: min(100% - var(--container-pad) * 2, var(--container-max));
    margin-inline: auto;
  }

  .footer__panel {
    display: flex;
    flex-direction: column;
    gap: clamp(2rem, 4vw, 2.5rem);
    padding: clamp(2rem, 5vw, 2.875rem) clamp(1.5rem, 5vw, 6rem);
    background: var(--color-surface);
    border-radius: var(--card-radius);
  }

  .footer__cta {
    display: flex;
    flex-direction: column;
    align-items: flex-start;
    gap: var(--space-header-content);
  }

  .footer__cta-copy {
    display: flex;
    flex-direction: column;
    gap: var(--space-label-title);
    max-width: 40.8125rem;
  }

  h2.footer__cta-title {
    margin: 0;
    font-family: var(--font-display);
    font-size: var(--text-section);
    font-weight: 400;
    line-height: var(--leading-tight);
    color: var(--color-text);
  }

  .footer__cta-text {
    margin: 0;
    font-family: var(--font-rokkitt);
    font-size: 20px;
    font-weight: 400;
    line-height: 1.2;
    color: var(--color-text);
  }

  .footer__cta-btn {
    flex-shrink: 0;
    min-height: var(--btn-height);
    padding: var(--btn-padding-y) var(--btn-padding-x);
    font-family: var(--font-accent);
    font-size: var(--btn-font-size);
    font-weight: var(--btn-font-weight);
    letter-spacing: var(--tracking-normal);
    text-transform: none;
  }

  .footer__rule {
    height: 1px;
    background: var(--color-border);
    opacity: 0.35;
  }

  .footer__body {
    display: flex;
    flex-direction: column;
    gap: clamp(2rem, 4vw, 2.5rem);
  }

  .footer__columns {
    display: flex;
    flex-direction: column;
    gap: 2rem;
  }

  .footer__col {
    min-width: 0;
    flex: 0 0 auto;
  }

  p.footer__col-title {
    margin: 0 0 var(--space-label-title);
    font-family: var(--font-display);
    font-size: var(--text-footer-heading);
    font-weight: 400;
    line-height: var(--leading-tight);
    color: var(--color-text);
  }

  .footer__links {
    list-style: none;
    margin: 0;
    padding: 0;
    display: flex;
    flex-direction: column;
    gap: 0;
  }

  a.footer__link {
    display: inline-block;
    padding-block: 0.25rem;
    font-family: var(--font-rokkitt);
    font-size: 16px;
    font-weight: 400;
    line-height: 1.2;
    color: var(--color-text-muted);
    transition: color 0.35s var(--ease-luxury);
  }

  .footer__link:hover,
  .footer__link:focus-visible {
    color: var(--color-accent);
  }

  .footer__copy {
    margin: 0;
    font-family: 'Trebuchet MS', 'Segoe UI', sans-serif;
    font-size: 14px;
    font-weight: 400;
    line-height: var(--leading-tight);
    text-align: center;
    color: var(--color-text-muted);
  }

  @media (min-width: 640px) {
    .footer__columns {
      flex-flow: row wrap;
      column-gap: clamp(3rem, 8vw, 7.5rem);
      row-gap: 2rem;
    }
  }

  @media (min-width: 900px) {
    .footer__cta {
      flex-direction: row;
      align-items: flex-start;
      justify-content: space-between;
      gap: var(--gap-card);
    }

    .footer__cta-btn {
      align-self: flex-start;
      margin-top: 0.25rem;
    }
  }
</style>
